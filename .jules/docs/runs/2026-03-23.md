# Run Log: 2026-03-23T10:13:54Z
Option A (recommended): Fix missing doctest compilation by changing rust,ignore to rust,no_run in README files. This fits the repo because preventing code block drift ensures examples are correct, improving trust and DX. The trade-off is slightly higher compilation time during `cargo test --doc`, but it's negligible here.
Option B: Look for missing doctest coverage for a common usage pattern. This is also good, but we noticed the low-hanging fruit of rust,ignore first.

Decision: Option A, as it directly solves an existing risk of examples silently drifting out of date with zero new code to be written.

- Found rust,ignore in README codeblocks.
- Changed to rust,no_run so compiler checks validity to prevent drift.
- Affected: crates/tokmd-core/README.md, crates/tokmd-format/README.md.
