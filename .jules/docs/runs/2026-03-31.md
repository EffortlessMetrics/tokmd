# Run Log: 2026-03-31

**Goal**: Maximize SRP-quality improvement per reviewer minute.

## Target Selection (Lane B - scout discovery)
The `tokmd` README contains examples for `tokmd run`, `tokmd diff` and `tokmd context` which were missing doctest verification in `crates/tokmd/tests/docs.rs`.

## Implementation Details
Updated `crates/tokmd/tests/docs.rs` to add test functions for these missing README examples.

- `recipe_run_analysis_receipt`: tests `tokmd run --analysis receipt --output-dir <tmp>`
- `recipe_diff_runs`: tests `tokmd diff <run_a> <run_b>` using deterministic run states in tmp directories to avoid git ref dependency issues.
- `recipe_context_budget`: tests `tokmd context --budget 128k --mode bundle --output <tmp>`

## Verification
- Ran `cargo test -p tokmd --test docs` successfully.
- Ran full `cargo test -p tokmd` successfully.
- Ran `cargo xtask gate` successfully.

## State Updates
- Appended run envelope at `.jules/docs/envelopes/<run_id>.json`
- Updated ledger at `.jules/docs/ledger.json`
