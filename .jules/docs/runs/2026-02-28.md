# Librarian Run 2026-02-28

**Run ID**: 20260228-104526

## Objective
Convert `tokmd-core` doctests from `no_run` / `ignore` to fully runnable examples as per agent instructions.

## Changes Made
- `crates/tokmd-core/src/lib.rs`: Modified doctest from `rust,no_run` to `rust`.
- `crates/tokmd-core/src/ffi.rs`: Modified doctest from `ignore` to `rust` and explicitly imported `tokmd_core::ffi::run_json`.

## Verifications
- Confirmed `cargo test --doc -p tokmd-core` completes successfully without failure.
- Confirmed `cargo test --workspace` run correctly.
- Confirmed `cargo clippy` and `cargo fmt` complete without issues.

## Telemetry
- **Risk Class**: Low. Modifying doc comment blocks to enable doctests execution.
