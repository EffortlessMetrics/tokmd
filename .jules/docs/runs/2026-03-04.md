# Run log 2026-03-04

- Addressed docs drift in `tokmd-core`.
- Replaced `rust,no_run` with `rust` in `crates/tokmd-core/README.md` to make doctests runnable.
- Verified compilation and passing of doctests via `cargo test -p tokmd-core --doc`.
