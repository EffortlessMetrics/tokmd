# Librarian Run 2026-04-02

## Selection
Lane B — scout discovery: Found missing doctest coverage for common usage patterns in `docs/tutorial.md` specifically for the `context`, `run`, and `diff` commands.

## Decision
- **Option A (recommended)**: Add integration tests in `crates/tokmd/tests/docs.rs` to verify CLI usage matches documentation. This prevents silent drift and makes the tutorial examples verifiable.
- **Option B**: Manually test the commands. This is not recommended as it doesn't provide automated regression protection.

Chosen **Option A** to ensure docs can't silently drift from the actual API behavior.

## Implementation
Added `recipe_context_bundle` and `recipe_run_and_diff` test functions to `crates/tokmd/tests/docs.rs`.

## Verification
- Ran `cargo test -p tokmd --test docs` and all tests passed.
- Ran `cargo fmt` and `cargo clippy -- -D warnings` and no issues were found.
