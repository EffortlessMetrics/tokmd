# Friction backlog

This is a small queue of recurring pain that scheduled work can burn down.

Rules:
- One item per file.
- Must include: pain, evidence, done-when.
- Keep it actionable. No “big rewrite” tickets.

Selection behavior comes from `.jules/policy/scheduled_tasks.json`.
