# Run 2026-01-30

- **ID**: 20260130T130854Z
- **Lane**: Scout (B)
- **Target**: `crates/tokmd-content/src/lib.rs`

## Findings
- Identified double-open pattern in `read_head_tail`.
- It opens the file, checks metadata, then calls `read_head` which opens it again.
- This is inefficient and introduces a minor TOCTOU race.

## Options
### Option A (Recommended)
Refactor `read_head` logic to be reusable with an open `File` handle.
- Create `read_head_from_file`.
- Use it in `read_head` and `read_head_tail`.
- Pros: Efficient, safer (same handle).
- Cons: Code change in `tokmd-content`.

### Option B
Ignore it.
- Pros: No code change.
- Cons: Inefficiency persists.

## Decision
Option A.
