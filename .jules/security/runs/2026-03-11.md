# Run 5cdea538-8a18-47d0-bbb7-b1f8560bcea8
Lane: scout
Target: crates/tokmd-cockpit/src/determinism.rs

## Findings
The `determinism.rs` test module contains 35 explicit `.unwrap()` calls which can panic.
Replacing them with `?` and returning `Result<(), Box<dyn std::error::Error>>` improves test hygiene and follows the project's guidance to burn-down panics.

## Options Considered
### Option A (recommended)
- What it is: Refactor test signatures to `fn test_xyz() -> Result<(), Box<dyn std::error::Error>>` and replace `.unwrap()` with `?`.
- Why it fits this repo: Strictly follows the memory instruction for test refactoring and burns down panic candidates.
- Trade-offs: Structure is improved, no negative velocity impact, slightly more verbose test signatures but much safer.

### Option B
- What it is: Replace `.unwrap()` with `.expect("...")` giving contextual messages.
- When to choose it instead: If the test harness doesn't support returning `Result` (which Rust's built-in does).
- Trade-offs: Still panics, just with a better message. Doesn't fully address the goal of removing panics.

## Decision
Option A. It aligns perfectly with the explicit project instructions.
