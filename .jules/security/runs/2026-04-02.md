# Run Log 2024-04-02 (7d373e3b-4ab2-4182-8dc0-be2d829777f2)

Lane: scout
Target: crates/tokmd-core/tests/error_w70.rs

## Findings
Discovered `.unwrap_or_default()` and `.unwrap()` calls in `crates/tokmd-core/tests/error_w70.rs` used on `Option`s and variables during test assertions. These generic calls obfuscate failures and add no value over standard `.unwrap()`.

## Options
### Option A
- What it is: Replace `.unwrap_or_default()` and `.unwrap()` with `.expect()` and highly descriptive error messages (distinguishing between "code" and "message").
- Why it fits this repo: Aligns with full quality stance of burning down panics/unwraps and making test failures explicit and fast.
- Trade-offs: Increases verbosity of assertions.

### Option B
- What it is: Do not touch tests, focus only on production code.
- When to choose it instead: If tests are not considered part of the security boundary.
- Trade-offs: Misses the mandated "treat unwrap/expect/panic as candidates everywhere (including tests)" rule.

## Decision
Choosing Option A because the rule explicitly mandates burning down unwraps in tests to improve Developer Experience (DX) and provide exact failure context.
