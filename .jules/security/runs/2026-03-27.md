# Sentinel Run Log - 2026-03-27

**Run ID**: 870d5eb5-ff84-4897-aec7-23fd9b25ef81
**Lane**: scout
**Target**: crates/tokmd/tests/determinism.rs unwrap burn-down

## Findings
Found ~20 `.unwrap()` calls on `serde_json::Value` in `crates/tokmd/tests/determinism.rs`.

## Options A/B
### Option A (recommended)
Replace `.unwrap()` with descriptive `.expect(...)` calls in tests to burn down panics and enforce quality.
### Option B
Do nothing and accept `.unwrap()` in tests.

**Decision**: Option A. Tests should fail with clear reasons to improve Developer Experience.
