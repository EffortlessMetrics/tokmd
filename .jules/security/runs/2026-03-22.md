# Sentinel Run - 20260322T122952Z
- Lane: scout
- Target: tokmd-math panics

## Findings
- Scout sweep revealed 15 unwraps in tokmd-math test modules.

## Options
### Option A
- Replace all min().unwrap() and last().unwrap() with safe fallback .unwrap_or(&0) or [0].
- Safe, scoped, burn-down.
### Option B
- Ignore them because they are tests.

## Decision
Option A. Sentinel lane enforces burn-down everywhere, including tests.
