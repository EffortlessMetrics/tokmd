# Run 2026-02-05

**Run ID**: `20260205-01`
**Lane**: B (Scout Discovery)
**Target**: `crates/tokmd-format/src/lib.rs` - Remove `unwrap()` in `normalize_scan_input` & CI Stabilization

## 🔎 Findings
1. Found `unwrap()` usage in `crates/tokmd-format/src/lib.rs`.
2. Previous submission failed CI: `tokmd-walk` tests failed in Nix sandbox due to missing git.
3. `cargo deny` flagged `bytes` crate (fixed by update).
4. `tokmd-model` normalization needed hardening (fixed previously).
5. `tokmd-git` integration tests also failed in Nix sandbox (fixed previously).

## 🧭 Options Considered
### Option A (recommended)
Fix `tokmd-walk` tests to be robust against missing git environment, similar to `tokmd-git`.
- **Pros**: Fixes CI failures.
- **Cons**: Tests are skipped in some environments.

## ✅ Decision
Option A. Updated `tokmd-walk` tests to skip if `git_available()` is false, and disable GPG signing.
