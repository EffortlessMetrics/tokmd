# Run 2026-02-05

**Run ID**: `20260205-01`
**Lane**: B (Scout Discovery)
**Target**: `crates/tokmd-format/src/lib.rs` - Remove `unwrap()` in `normalize_scan_input`

## 🔎 Findings
Found `unwrap()` usage in `crates/tokmd-format/src/lib.rs` within `normalize_scan_input`.
While the logic `while s.starts_with("./") { s = s.strip_prefix("./").unwrap().to_string(); }` is technically safe, it relies on implicit invariants and uses a panic-inducing method (`unwrap`) in production code.
This violates the "Full quality stance: treat unwrap/expect/panic as candidates everywhere" rule.

## 🧭 Options Considered
### Option A (recommended)
Replace the `starts_with` + `unwrap` loop with a `while let` pattern binding.
- **Pros**: Removes `unwrap()`, more idiomatic Rust, robust against any potential future changes to `strip_prefix` behavior (unlikely but good practice).
- **Cons**: None.

### Option B
Status quo.
- **Pros**: Less churn.
- **Cons**: Leaves `unwrap()` in production code, which is a signal we want to eliminate.

## ✅ Decision
Option A. Remove the `unwrap()` to align with the "Burn-down is the goal" policy.
