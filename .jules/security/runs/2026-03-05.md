# Sentinel Run 2026-03-05

## Target
Lane: Scout Discovery
Target: `crates/tokmd/src/analysis_utils.rs` panic-inducing `.expect()` calls.

## Findings
Found an instance of `.expect()` in `crates/tokmd/src/analysis_utils.rs` which can lead to panics. Replacing them with proper error handling using exhaustive matching will improve reliability.

## Options Considered

### Option A (recommended)
Replace `.unwrap()` with exhaustive matching for `map_preset`.
- **Trade-offs:** Exhaustive matching is inherently safer as it checks at compile-time.

### Option B
Change all `.unwrap()` to `match` expressions and log the error, skipping the faulty piece.
- **Trade-offs:** Can leave the output incomplete silently. Less visible to the user but avoids panics.

## Decision
Option A. We want to fail gracefully or avoid panicking with compile-time checked logic, instead of an abrupt panic at runtime.

## Changes
- `crates/tokmd/src/analysis_utils.rs`: Changed `map_preset` from string formatting and parsing to an exhaustive match statement.
