# Bolt Run Log 2026-03-01

## Read
- CI + docs
- Cargo.toml

## Lane
TBD

## Target
TBD

## Proof
TBD

## Receipts
TBD

## Lane
Scout

## Target
module_key string allocations in tokmd-module-key

## Proof
Benchmark Output: \`cargo bench -p tokmd-module-key\` showed a 25% performance improvement (from ~1.01µs to ~665ns per iteration of 9 paths).

## Decision
Option A (recommended): Refactor `module_key` to avoid initial unconditional `replace` allocation, checking for `contains` first, and using string slices (`&str`) over `String` ownership passing where possible. Fits well because path string allocations are very hot during scanning/aggregating thousands of files.
Decision: Implemented Option A.

## Receipts
- `cargo bench -p tokmd-module-key`
- `cargo test -p tokmd-module-key`
- `cargo clippy -p tokmd-module-key -- -D warnings`
