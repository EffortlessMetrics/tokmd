# Bolt Run Log
- CI/Docs read: Found `render_file_table` missing capacity hint, leading to unnecessary allocations.
- Lane: scout
- Target: `crates/tokmd-analysis-format/src/lib.rs:render_file_table`
- Proof: structural
- Options considered:
  - Option A: Add `String::with_capacity((rows.len() + 3) * 80)` hint and use `writeln!` instead of `push_str(&format!(...))`. Avoids intermediate string allocations.
  - Option B: Add `String::with_capacity` but keep `format!()`.
  Chosen: Option A for best performance.
- Receipts: pending
