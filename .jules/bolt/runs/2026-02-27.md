# Run 20260227-132931

## Context
Read: CLAUDE.md, CONTRIBUTING.md, AGENTS.md

## Lane
Scout Discovery (Lane B)

## Target
`normalize_path` in `crates/tokmd-model/src/lib.rs`
The function is hot (called for every file report) and does string allocations/replacements.
Optimization opportunity:
1. Avoid `Cow` conversions where unnecessary.
2. Avoid `.to_string_lossy()` which allocates if UTF-8 is valid (it usually is).
3. Reduce string cloning when no changes are needed.
4. Fast-path check for slashes before replacement.

## Proof Method
Existing benchmark example: `cargo run --release --example bench_normalize`
Baseline: ~65ns per call.

## Decisions
Option A (Recommended): Optimize `normalize_path` logic to minimize allocations.
- Replace `to_string_lossy()` with `to_str()` check.
- Use `Path::to_str` and work with `&str` directly where possible.
- Avoid `replace` if no backslashes exist.
- Keep output deterministic and compatible.

Option B: Do nothing.
- Status quo.

Decision: Option A.

## Receipts
- Baseline bench run: 65ns/call.
