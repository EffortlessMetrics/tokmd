# Wrong Repo Intake

The `v1.12.0` release workflow imports work from `tokmd-swarm` into `tokmd` via merge commit. Any patch submitted directly to `tokmd` for this surface will be rejected as wrong-repo intake. Work must be submitted as a narrow PR against `tokmd-swarm`.
