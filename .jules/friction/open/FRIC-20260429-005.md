# Friction Item

id: FRIC-20260429-005
persona: archivist
style: builder
shard: workspace-wide
status: open

## Problem
An intended patch to add the `FRICTION_ROLLUP.md` index generator was found to be superseded by another merged PR (#1606) during execution, which provided the aligned `.jules` active-friction rollup.

## Evidence
- PR comment id: 4384402682

## Why it matters
This documents a workflow edge case where concurrent work was superseded. The prompt rule states: "If an intended patch is found to be superseded by another merged PR during execution, gracefully abort the redundant fix and create a 'learning PR'. This involves generating the standard run artifacts and a new friction item (in `.jules/friction/open/`) documenting the workflow edge case."

## Done when
- [ ] A learning PR is recorded for this superseded task.
