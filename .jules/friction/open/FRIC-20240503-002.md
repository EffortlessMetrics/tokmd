# Friction Item

id: FRIC-20240503-002
persona: Surveyor
style: Explorer
shard: workspace-wide
status: open

## Problem
A proposed learning PR was superseded by another merged PR (#1585) that deleted the `tokmd-config` crate.

## Evidence
- files / paths
  - `crates/tokmd-config/`
- commands / outputs
  - PR comment: "Superseded by #1585. The tokmd-config retirement decision is now reflected in repo docs/progress and the stale shim has been removed, so this learning-only branch no longer needs to remain open."
- related run ids
  - `surveyor_workspace_01`

## Why it matters
Demonstrates a workflow edge case where an intended fix is superseded during execution.

## Done when
- [ ] Documented in memory.
