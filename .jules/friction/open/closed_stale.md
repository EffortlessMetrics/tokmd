# Friction: PR closed as stale

The PR was closed as stale/deferred because it predates the completed 1.15.0 and 1.15.1 releases. The work has been deferred to avoid blocking the release queue and should be salvaged from this branch if the fix is still desired.
