# Friction Item

id: FRIC-20240518-001
persona: Gatekeeper
style: Prover
shard: core-pipeline
status: open

## Problem
No deterministic output drift was found in `tokmd-model` or `tokmd-format` sorting invariants; all existing properties natively rely on standard `.sort_by(|a, b| b.code.cmp(&a.code).then_with(...))` and `determinism` tests accurately match these guarantees. The suite is currently rock solid and adding arbitrary tests or snapshot regenerations did not uncover regressions. While we searched for edge cases like tie-breaking sort order stability, all tested paths already behave optimally and tests cover it.

## Evidence
- files / paths: `crates/tokmd/tests/determinism.rs`, `crates/tokmd/tests/determinism_regression.rs`
- commands / outputs: `cargo test -p tokmd --test determinism_regression` (26 passed), `cargo test -p tokmd --test determinism_hardening` (16 passed)

## Why it matters
To track that no forced fix was necessary, validating the current resilience of the sorting and tiebreaking pipeline outputs against drift.

## Done when
- [ ] Reviewer acknowledges pipeline stability
