# Friction: Docs are already synced
The current documentation `docs/reference-cli.md` is strictly synced with the binary CLI definitions, and `cargo xtask docs --check` passes cleanly. There is no factual docs/example drift to correct. Attempting to artificially update it would violate the "no tone-only prose rewrites" rule.
