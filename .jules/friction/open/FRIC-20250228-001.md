---
title: "Sentinel: Redaction correctness and leakage prevention are already implemented"
date: "2025-02-28"
tags: ["sentinel", "core-pipeline", "security-boundary"]
---

# Problem
The run assignment requested a security-significant hardening improvement focused on redaction correctness and leakage prevention in the `core-pipeline` shard. However, `tokmd-format` already robustly implements these constraints.

# Impact
Attempting to fulfill the prompt with a patch would result in a "fake fix" or redundant test coverage, wasting reviewer time and bloating the test suite.

# Context
- `crates/tokmd-format/src/redact/mod.rs` and `crates/tokmd-format/src/redact/extensions.rs` already correctly implement the required safe extension preservation and hash-based path redaction.
- `crates/tokmd-format/tests/test_redaction_leak.rs` already proves properties like `.tar.gz` preservation, `.json.rs` rejection, and `.rs.bak` redaction.
