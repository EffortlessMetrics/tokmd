id: FRIC-20250430-001
persona: archivist
style: builder
shard: analysis-stack
status: open

## Problem
A planned structural optimization to eliminate thousands of string allocations in derived report aggregations (`BTreeMap<&str, ...>` instead of `BTreeMap<String, ...>`) was superseded by an external pull request (#1608) during execution. The current patch was correctly formed and tested, but was blocked from merging by the concurrent system update.

## Evidence
- files: `crates/tokmd-analysis/src/derived/mod.rs`
- related run ids: `bolt-derived-alloc-opt`
- Comment on PR: `Superseded by #1608, which folded the useful borrowed-key derived-report allocation reductions into a current-main keeper.`

## Why it matters
Demonstrates a race condition in asynchronous agent pipelines where structurally correct patches are obsoleted by human or other automated PRs. Documenting this ensures that the run artifacts are preserved as a "learning PR" to track workflow edge cases, avoiding dead code push and preventing silent failure states.

## Done when
- [ ] A learning PR is successfully created for this friction item.
- [ ] The run packet artifacts reflect the aborted nature of the superseded fix.
