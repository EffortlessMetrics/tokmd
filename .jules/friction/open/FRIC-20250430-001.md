---
id: FRIC-20250430-001
persona: Librarian
style: Builder
shard: tooling-governance
status: open
---

## Problem
The missing `CONTEXT_BUNDLE_SCHEMA_VERSION` test in `xtask/tests/docs_w43.rs` was intended to be patched, but was superseded by #1604 which merged the aligned schema drift test coverage on current main. This caused a redundant fix attempt.

## Evidence
- path: `xtask/tests/docs_w43.rs`
- PR comment indicating supersedence: "Superseded by #1604, which merged the aligned schema drift test coverage, including the missing W43 context-bundle check, on current main."
- Run ID: `librarian_docs_examples_1`

## Why it matters
Redundant fixes waste agent execution time. Finding an automated way to detect if a specific drift gap is already patched on main before initiating the run could save effort.

## Done when
- [ ] Determine if pre-flight checks against `origin/main` could detect recently patched drift test gaps.
- [ ] Update Librarian runbook or scaffolding to abort early if the intended docs/test patch matches an already-open or recently-merged PR pattern.
