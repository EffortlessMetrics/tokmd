# Obsolete PR Attempt

Tried to remove `pyo3-build-config` from `crates/tokmd-python/Cargo.toml` as an Auditor improvement based on `cargo-machete` output.

However, the resulting PR was superseded by #1350 which already merged the cleanup on current main.
