---
id: mutant_superseded_env_token
persona: Mutant
style: Prover
shard: core-pipeline
status: open
---
# Workflow Collision: Superseded PR

The intended mutant patch targeting the missing `--split-string` and `--ignore-environment` assertions in `crates/tokmd-model/src/lib.rs` (specifically the `env_interpreter_token` function) was superseded by PR #1905 while work was underway. The patch was gracefully aborted.
