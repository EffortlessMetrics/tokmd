{
  "run_id": "2026-02-15-gatekeeper-view-determinism",
  "goal": "Verify and lock deterministic ordering for view profiles in configuration",
  "lane": "scout",
  "decision": "Option A (Use BTreeMap instead of HashMap)",
  "changes": [
    "crates/tokmd-config/src/lib.rs",
    "crates/tokmd-config/tests/determinism.rs"
  ],
  "verification": {
    "test": "cargo test -p tokmd-config --test determinism",
    "workspace": "cargo test -p tokmd-config"
  }
}
