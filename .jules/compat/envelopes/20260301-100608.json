{
  "run_id": "20260301-100608",
  "lane": "scout discovery",
  "issue": "Path normalization test failure in tokmd-analysis-entropy",
  "receipts": [
    "cargo test --workspace --no-default-features",
    "cargo test -p tokmd-analysis-entropy --test bdd",
    "INSTA_UPDATE=always cargo test -p tokmd-model --test snapshots --workspace --no-default-features --exclude tokmd-python --exclude tokmd-node",
    "cargo test --workspace --all-features --exclude tokmd-python --exclude tokmd-node"
  ]
}
