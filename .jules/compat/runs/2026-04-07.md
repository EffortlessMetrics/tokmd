# Compat Run: 2026-04-07

## Target
`--no-default-features` build failure / matrix interaction.

## Actions
Added `#![cfg(feature = "git")]` to the top of `context` integration test files (`crates/tokmd/tests/context_cli_w73.rs`, `crates/tokmd/tests/context_e2e.rs`, `crates/tokmd/tests/context_handoff_deep.rs`, `crates/tokmd/tests/context_w71.rs`, `crates/tokmd/tests/deep_context_handoff_w51.rs`).

## Verification
- Verified `cargo test -p tokmd --no-default-features`
- Verified `cargo test -p tokmd --all-features`

## Outcome
PASS
