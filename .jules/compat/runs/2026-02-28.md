# Run 20260228-100237 (2026-02-28)

## Goal
Fix `--no-default-features` build failures in the CLI.

## Action
Added missing `#[cfg(feature = "git")]` attributes to:
- `crates/tokmd/src/commands/baseline.rs`
- `crates/tokmd/src/commands/cockpit.rs`
- `crates/tokmd/src/commands/sensor.rs`
- `crates/tokmd/tests/run_diff.rs` (for `alias-tok` feature)

## Verification
- `cargo check --no-default-features --all-targets --workspace` passes.
- `cargo check --all-features --all-targets --workspace` passes.
