# Compat Run: tokmd-cockpit --no-default-features
- **Run ID**: 6d0638a9-899c-4f54-9012-a5c6197aedae
- **Target**: tokmd-cockpit
- **Lane**: Scout discovery
- **Action**: Added `#[cfg(feature = "git")]` to various unused imports and functions in `tokmd-cockpit` to resolve dead code and unused import warnings when building with `--no-default-features`.
- **Receipts**:
  - `cargo check --workspace --exclude tokmd-python --exclude tokmd-node --no-default-features` passes with 0 warnings.
  - `cargo test -p tokmd-cockpit --no-default-features` passes.
  - `cargo check -p tokmd-cockpit --all-features` passes.
