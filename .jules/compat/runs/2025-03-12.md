# Run 780d963a-f594-4b58-ad83-f5dae053e367
- **Persona:** Compat
- **Target:** crates/tokmd-cockpit/src/lib.rs

## Issue
The crate failed to build and verify with `--no-default-features` due to git-specific code generating unused imports and dead code warnings.

## Solution
Wrapped the specific git-related functionality (functions `compute_overall_status`, `run_schema_diff`, etc., along with their imports and specific tests) in `#[cfg(feature = "git")]`.

## Verification
Executed `cargo clippy`, `cargo check`, and `cargo test` for default, no-default, and all features with `-D warnings` on `tokmd-cockpit` successfully.
