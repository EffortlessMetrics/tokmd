# Run 5ff823db-a0c2-4add-bff4-84a451a8a4d7

**Date**: 2023-10-27
**Persona**: Auditor
**Target**: `tokmd-sensor`
**Action**: Removed unused `tokmd-git` dependency and `git` feature

## Context
Running `cargo machete` identified `tokmd-git` as an unused dependency in `crates/tokmd-sensor/Cargo.toml`. Since `tokmd-git` was the only dependency required for the `git` feature in `tokmd-sensor`, the `git` feature was also removed to improve dependency hygiene and reduce compile surface.

## Verification
- `cargo test -p tokmd-sensor` passed.
- `cargo xtask gate` passed.

## Receipts
- `cargo machete` output identifying unused `tokmd-git`.
- `cargo test -p tokmd-sensor` succeeded.
- `cargo xtask gate` succeeded.
