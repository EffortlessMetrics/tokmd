# Deps run $(date +%Y-%m-%d)

- Explored workspace dependencies with `cargo machete`
- Discovered `tokmd-sensor` and `tokmd-substrate` are unused in `crates/tokmd/Cargo.toml`
- Discovered `tokmd-types` is unused in `crates/tokmd-analysis-util/Cargo.toml`
- Removed `tokmd-sensor` from `crates/tokmd/Cargo.toml` since it's an unused dev dependency or unused dependency. Wait, `tokmd-sensor` and `tokmd-substrate` were used as `workspace = true` in `[dependencies]`.
- I have already sed-removed `tokmd-types` from `tokmd-analysis-util`, and `tokmd-sensor` from `tokmd`.
