# Deps Run Log: 2026-04-03

**Persona:** Auditor 🧮
**Lane:** B - scout discovery
**Target:** Remove `uselesskey` dev-dependency

## Options Considered

### Option A (recommended)
- Remove `uselesskey` dependency from `tokmd-analysis` and `tokmd-analysis-entropy`.
- Generate the static `.pk8` binary fixtures for the testing and use `include_bytes!` instead.
- This is a safe and high-signal hygiene improvement since `uselesskey` is only used to generate deterministic test fixtures, which don't need runtime entropy.

### Option B
- Continue generating the test fixtures with `uselesskey` at runtime.
- Wait for a potential future refactor or removal.

## Decision
Option A was chosen. We generated the RSA PKCS8 DER binary fixtures locally, committed them as test artifacts under `tests/fixtures/generated/private-key.pk8`, and removed `uselesskey` entirely from the workspace dev-dependencies.

## Verification
Ran `cargo test` and all verifications. Tests passed successfully.
