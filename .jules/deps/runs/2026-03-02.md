# Auditor Run: 2026-03-02

## Goal
Improve dependency hygiene by removing unused dependencies from the project.

## Strategy
Option B: Scout discovery

## Action
Removed `tokmd-sensor` and `tokmd-substrate` from `tokmd` crate dependencies as identified by `cargo-machete`.

## Receipts
```
cargo-machete found the following unused dependencies in this directory:
tokmd -- ./crates/tokmd/Cargo.toml:
	tokmd-sensor
	tokmd-substrate
```
