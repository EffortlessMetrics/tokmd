# Run Log - Auditor

**Date:** 2026-04-08T11:42:08Z
**Lane:** scout
**Target:** tokmd-analysis and tokmd-analysis-entropy

## Options Considered

### Option A
- **What:** Try to remove `tokei` dependency and substitute it with something lighter.
- **Why Not:** `tokei` is deeply integrated into `tokmd-scan` and provides the core line-of-code metrics. Removing it would require rewriting massive amounts of functionality or losing key functionality.

### Option B (Selected)
- **What:** Remove `uselesskey` from `tokmd-analysis` and `tokmd-analysis-entropy` `[dev-dependencies]` by pre-generating the RSA DER key fixtures and loading them statically with `include_bytes!`.
- **Why:** Aligns perfectly with the stated guidelines: "When performing dependency hygiene... to remove dev-dependencies used exclusively for generating test data (like cryptography or high-entropy data), do not delete the tests themselves. Instead, replace the dynamic runtime generation with pre-computed static binary test fixtures". This avoids the compilation and dependency graph cost of `uselesskey` just for test execution.

## Action Taken
- Ran a local script to dump the exact bytes of the `uselesskey` RSA derivations into `.pk8` files.
- Moved these files into the `tests/fixtures/generated/` folder for both crates.
- Rewrote the test files (`uselesskey_runtime_fixtures.rs` and `uselesskey_security_pipeline.rs`) to load these static bytes instead of invoking `uselesskey::Factory`.
- Removed the `uselesskey` dependency from `tokmd-analysis`, `tokmd-analysis-entropy`, and workspace root.

## Results
- Cargo tests passed.
- Reclaimed compilation time and shrunk the dev dependency tree.
