## Run 20260202-proptest (Auditor)

### Receipts
- **Dependency Check**: `cargo tree -d` confirmed elimination of `rand 0.9` tree.
- **Tests**: `cargo test --workspace` passed (100% success).
- **Lint**: `cargo clippy` clean.
- **Format**: `cargo fmt` checked.

### Changes
- Centralized `proptest` to `=1.5.0` in `Cargo.toml`.
- Updated 15 child crates to use `workspace = true`.
- Removed `rand 0.9`, `getrandom 0.3`, `rand_chacha 0.9` duplicates.
