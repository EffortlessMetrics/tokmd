# PR Glass Cockpit

Make review boring. Make truth cheap.

## 💡 Summary
Removed unused `serde` dev-dependency from `tokmd-analysis`.

## 🎯 Why (perf bottleneck)
Unnecessary dependencies bloat the lockfile and can theoretically increase compile times or conflict surfaces, violating dependency hygiene.

## 📊 Proof (before/after)
Structural proof: The crate builds and tests pass cleanly without `serde` as a dev-dependency.

## 🧭 Options considered
### Option A (recommended)
- What it is: Remove unused `serde` dev-dependency.
- Why it fits this repo: Strictly improves dependency hygiene.
- Trade-offs: Velocity / Structure / Governance. Safe removal.

### Option B
- What it is: Investigate Python/Node build config unused warnings.
- When to choose it instead: If a larger cleanup footprint was desired.
- Trade-offs: Higher risk of breaking CI for build scripts.

## ✅ Decision
Option A. It's a textbook, low-risk hygiene improvement.

## 🧱 Changes made (SRP)
- `crates/tokmd-analysis/Cargo.toml`

## 🧪 Verification receipts
- `cargo machete` flagged `serde` as unused in `tokmd-analysis`.
- `cargo test -p tokmd-analysis --all-features` passed.
- `cargo clippy` passed.

## 🧭 Telemetry
- Change shape: Removal
- Blast radius: `tokmd-analysis`
- Risk class: Low
- Rollback: Re-add dependency
- Merge-confidence gates: fmt, clippy, test

## 🗂️ .jules updates
- Appended run entry to `.jules/deps/ledger.json`.

## 📝 Notes (freeform)
N/A

## 🔜 Follow-ups
N/A
