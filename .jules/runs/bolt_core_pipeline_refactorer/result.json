{
  "outcome": "pr-ready-patch",
  "files_changed": [
    "crates/tokmd-model/src/lib.rs",
    "crates/tokmd-types/src/lib.rs"
  ],
  "reason": "Replaced BTreeMap/BTreeSet with HashMap/HashSet in hot-path aggregations for language and module reporting. The new pipeline uses standard O(1) hash maps to avoid logarithmic cost per insertion and ensures the final result remains deterministic by explicitly sorting. Tested with criterion and achieved ~1.83x speedup."
}
