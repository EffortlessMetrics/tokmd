# Option A: Add Friction Rollup support to build_index.py
I will modify `.jules/bin/build_index.py` so that it parses `.jules/friction/open/*.md` files and outputs them to `.jules/index/generated/FRICTION_ROLLUP.md` as an index.
The template frontmatter specifies:
id: FRIC-...
persona: ...
style: ...
shard: ...
status: open

I will use Python's `re` module or simple string parsing to extract these frontmatter fields and generate a Markdown table.
This fulfills the Archivist persona target: "summarize per-run packets into generated indexes/rollups" and ensures compliance with the rule that `FRICTION_ROLLUP.md` index should be generated by `.jules/bin/build_index.py`.

# Decision
I'll choose Option A.
