{
  "status": "success",
  "outcome": "patch",
  "title": "archivist: natively aggregate historical ledger runs in build_index.py 🗃️",
  "summary": "Updated `.jules/bin/build_index.py` to aggregate per-run packets natively from historical ledgers in `.jules/docs/` and `.jules/quality/` without mutating original history.",
  "target_paths": [
    ".jules/bin/build_index.py",
    ".jules/index/generated/RUNS_ROLLUP.md"
  ],
  "gates_run": [
    "python3 .jules/bin/build_index.py"
  ]
}
