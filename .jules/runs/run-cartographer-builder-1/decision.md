The intended patch to fix factual drift regarding `in-browser receipt generation` in `docs/NOW.md` and the missing `capabilities` design pattern in `docs/design.md` was found to be superseded by another merged PR (#1588).

Following the runbook instructions, I am gracefully aborting the redundant fix and falling back to creating a learning PR containing the full per-run packet and documenting the workflow edge case in a friction item.
