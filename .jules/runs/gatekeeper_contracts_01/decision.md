# Decision

## Option A (recommended)
Fix the `ci-lane-whitelist.toml` policy file to include the missing CI jobs reported by `cargo xtask ci-lane-whitelist`.

## Decision
Choose Option A to fix semantic drift.
