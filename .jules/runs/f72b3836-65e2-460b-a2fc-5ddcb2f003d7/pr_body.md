## Context
The `docs/implementation-plan.md` file drifted from the reality reflected in `ROADMAP.md` and the shipped codebase. It lacked the completed `Phase 4f: Language Bindings` (`tokmd-python`, `tokmd-node`) and incorrectly marked several WASM core capabilities as incomplete.

## Changes
- Updated `docs/implementation-plan.md` to check off completed WASM Phase 5 work items.
- Inserted `Phase 4f: Language Bindings (FFI)` to align with `ROADMAP.md` and actual `tokmd-python` and `tokmd-node` shipments.

## Justification
Aligns the roadmap design and architecture documentation with the current state of the codebase.
