## Decision

**Problem:** The browser runner cross-realm error UX is superseded by upstream changes. The previous logic directly touched browser-runner error translation handling. But #1170 has been merged, so this effort overlaps.

### ✅ Decision
Cancel this work and write a learning PR artifact instead, indicating the conflict.
