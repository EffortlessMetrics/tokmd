# Dual-Repo Topology Note
Palette must remember that functional DX improvements to the FFI boundaries (`tokmd-envelope`, bindings) cannot be merged directly into `EffortlessMetrics/tokmd`. Work must be scoped and pushed to `tokmd-swarm` instead. This repository is strictly for release publication and merge-commit intake.
