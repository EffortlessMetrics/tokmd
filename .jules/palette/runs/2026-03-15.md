# Palette Run 2026-03-15

## Read
- Scanned tokmd-core source code for expect/unwrap usages.
- Identified multiple usages of expect in tests within `crates/tokmd-core/src/ffi.rs`.

## Selected Lane
scout

## Target
Refactor `expect` calls to use the `?` operator in `crates/tokmd-core/src/ffi.rs` tests.

## Options
### Option A
Refactor the tests in `crates/tokmd-core/src/ffi.rs` to return `Result<(), Box<dyn std::error::Error>>` and replace `.expect()` with the `?` operator.

### Option B
Leave the `.expect()` calls as they are since they are in tests.

## Decision
Choosing Option A to improve code quality and reduce panics, as requested by the persona guidelines.

## Findings
Refactored multiple tests in `crates/tokmd-core/src/ffi.rs` to return `Result<(), Box<dyn std::error::Error>>` and use the `?` operator instead of `.expect()` or `.unwrap()`.

## Receipts
(Will be filled by verification steps)
