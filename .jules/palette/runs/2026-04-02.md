# Run Log - 2026-04-02

## Context
Read CI and docs. Discovered many poor `.expect("should exist")` messages in `crates/tokmd/src/context_pack.rs` which violates the high quality bar (unhelpful unwrap/expect/panic messages).

## Lane & Target
- **Lane**: scout
- **Target**: Improve generic `.expect("should exist")` messages in `crates/tokmd/src/context_pack.rs` tests.

## Options
### Option A
- Replace generic `.expect("should exist")` with specific, context-aware messages based on what the test is asserting (e.g. `expect("parse_budget should successfully parse valid suffix 'k'")`).

### Option B
- Refactor the tests to return `anyhow::Result<()>` and use the `?` operator.
  *(Rejected: test assertions like `assert_eq!` are idiomatic with explicit unwraps or `Some(...)` matching, but we shouldn't bulk-rewrite to `?` for simple parser asserts since it reduces the specificity of where it failed compared to an expect message on the exact line.)*

**Decision**: Option A. It's an isolated SRP change targeting DX (developer experience when tests fail).

## Findings
I replaced all instances of `.expect("should exist")` in `crates/tokmd/src/context_pack.rs` tests with context-specific messages describing exactly what property or path was expected to exist.

## Receipts
- `cargo test -p tokmd --verbose` - PASS (test result: ok. 43 passed)
- `cargo clippy -- -D warnings` - PASS (Finished `dev` profile)
- `cargo fmt -- --check` - PASS
