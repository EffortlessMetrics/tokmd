# Run Log: 2025-02-19

## Context
- Read: CI definitions, CLAUDE.md, CONTRIBUTING.md, AGENTS.md
- Gates: build (PASS), test (pending), fmt (PASS), clippy (PASS)

## Selection
- Lane: Scout Discovery
- Target: `crates/tokmd/src/config.rs`: `try_load_toml` silently suppresses parsing errors.

## Findings
- `tokmd.toml` parsing errors are silently ignored, leading to confusing behavior where configuration is not applied.
- `tokmd lang -v` with a broken `tokmd.toml` shows no warning.

## Receipts
- Reproduction: `echo "invalid = toml" > tokmd.toml && ./target/debug/tokmd lang` ran successfully without error.

## Decision
- **Option A**: Warn on stderr.
- **Option B (Selected)**: Error and exit. Fail fast prevents "silent failure" debugging. Users expect config to be applied if present.

## Plan
1. Modify `try_load_toml` to return `Result<Option<...>>`.
2. Modify `discover_toml_config` to propagate errors.
3. Modify `load_config` to return `Result<ConfigContext>`.
4. Update `run` to handle the error.
5. Verify with broken and valid configs.
