# Palette Run

## Read
- CI definition: \`cargo build --verbose\`, \`CI=true cargo test --verbose\`, \`cargo fmt -- --check\`, \`cargo clippy -- -D warnings\`
- docs: \`crates/tokmd/src/context_pack.rs\` and \`crates/tokmd/src/commands/sensor.rs\`

## Options A/B
### Option A
- Target: Replace non-descriptive `.expect("should exist")` calls in tests with highly descriptive messages indicating what specific invariant failed.
- Why: Tests should fail fast with descriptive messages to improve DX when debugging.
### Option B
- Target: Improve CLI error messages in `tokmd check-ignore` or `tokmd analyze --explain`.
- Why: Provide better guidance for users when they input invalid arguments.

## Decision
Option A was chosen. After inspecting the codebase, CLI error messages are already quite comprehensive (e.g. `error_hints.rs` adding context). However, there are over 40 generic `.expect("should exist")` calls in `context_pack.rs` tests. Fixing these provides a substantial DX improvement for contributors debugging test failures, and satisfies the prompt's rule: "treat unwrap/expect/panic as candidates for cleanup if you touch those areas".

Lane: scout
Target: generic expect messages in tests
Findings: Found generic .expect("should exist") and .expect("should serialize") messages in tokmd/src/context_pack.rs and tokmd/src/commands/sensor.rs. Replaced with descriptive strings explaining invariants.
Receipts: PASS
