# Run Log 2026-04-05

## Read
- GitHub Workflows / Cargo CI configs
- `crates/tokmd-core/tests/error_w70.rs`

## Selected Lane
- **Lane**: scout

## Target
- **Target**: tokmd-core error_w70.rs unwrap_or_default() removal

## Options Considered
### Option A (recommended)
Replace `.unwrap_or_default()` with explicit `.expect("error.code must be a string")` and `.expect("error.message must be a string")` calls.
This fits the repo by improving developer experience (DX) when a test fails. A silent fallback to empty string on missing or invalid JSON obscures the real root cause, leading to confusing test failures downstream. Explicit expects create loud, contextual failure points.

### Option B
Use the `anyhow` crate or return `Result` from tests.
This is heavier, and proptest/macros in other parts of the repository often do not support `Result` signatures cleanly. Also, these assertions explicitly verify JSON invariants returned by the FFI boundary, which *must* be met.

**Decision**: Option A. It's targeted, lightweight, and eliminates the silent failure mode by introducing proper test assertions on JSON structure.

## Findings
Modified `crates/tokmd-core/tests/error_w70.rs` to replace 11 instances of `.unwrap_or_default()` when parsing string values from JSON.

## Receipts
(To be filled after verification)
